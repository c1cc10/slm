# The Docker image that will be used to build your app
image: node:lts
create-pages:
pages:
  stage: deploy
  script:
    - cp -r docs/ public/
  artifacts:
    paths:
      - public
  only:
    - mainer that np